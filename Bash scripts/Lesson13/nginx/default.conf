server {
    listen 80;  # Listen on default HTTP port

    location / {
        proxy_pass http://web:5000;  # Forward all requests to the Flask app
        proxy_set_header Host $host;  # Preserve original Host header
        proxy_set_header X-Real-IP $remote_addr;  # Preserve client's IP
    }
}
